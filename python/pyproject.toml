[project]
name = "python-homeassesment"
version = "0.1.0"
description = "Simple Flask app"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "gunicorn>=23.0",
    "pandas>=2.0",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = []
